{
  "name": "loan-tracker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "check": "node --check js/app.js && node --check js/receipt-parser.js && node --check js/sync-version.js && node --check js/loan-analytics.js && node --check js/encrypted-backup.js && node tests/verify-build.mjs",
    "test": "node --test tests/*.test.cjs",
    "e2e": "playwright test",
    "ci": "npm run check && npm test && npm run e2e"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.1"
  }
}
