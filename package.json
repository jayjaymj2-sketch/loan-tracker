{
  "name": "loan-tracker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "check": "node --check js/app.js && node --check js/receipt-parser.js && node --check js/sync-version.js && node tests/verify-build.mjs",
    "test": "node --test tests/*.test.cjs",
    "ci": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
